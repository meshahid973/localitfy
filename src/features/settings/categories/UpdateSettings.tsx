import type { Settings } from "../settings.types";
import type { UpdateSetting } from "../settings.shared";
import { ToggleRow } from "../settings.shared";
export default function UpdateSettings({ settings, updateSetting }: { settings: Settings; updateSetting: UpdateSetting }) {
  return <section className="settingsCategoryPage settingsDeclutterPageV491 settingsUpdatesPage settingsUpdatesCleanV491" aria-label="Update settings"><div className="settingsCategoryHeader settingsCategoryHeaderCleanV491"><div><p className="eyebrow">updates</p><h4>simple update notice</h4></div><span>{settings.autoUpdateEnabled ? "checks on" : "checks off"}</span></div><div className="settingsPanelCard settingsFocusPanelV491 settingsUpdateBehaviorPanel"><div className="settingsPanelHeader"><div><strong>Update behavior</strong><span>The top update island stays simple: update available, download update, close.</span></div></div><div className="settingsMiniGrid"><ToggleRow label="Update checks" help="Checks for new releases while the app is open." checked={settings.autoUpdateEnabled} onChange={(value) => void updateSetting("autoUpdateEnabled", value)} /><ToggleRow label="Notify only" help="Shows an update message instead of installing automatically." checked={settings.autoUpdateNotifyOnly} onChange={(value) => void updateSetting("autoUpdateNotifyOnly", value)} /></div></div></section>;
}
